import React, { Component } from 'react'
import {
   AppRegistry,
   View
} from 'react-native'
import Router from './components/HomeContainer'

class Navigation extends Component {
   render() {
      return (
         <HomeContainer />
      )
   }
}

AppRegistry.registerComponent('Switch', () => switch (expression) {
  case expression:

    break;
  default:

})
